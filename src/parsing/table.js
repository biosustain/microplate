import XLSX from 'xlsx';

/**
 * Like Layout, but for tables that are not plate layouts.
 */
class Table {

    static async parse(sheet, {
        required=[],
        converters={},
        validators={},
        parallel=false
        } = {}) {


    }

    append(contents) {

    }

    toSheet() {

    }
}